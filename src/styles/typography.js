import styled from "styled-components";

//General Font Sizes are set at the base in the global styles.
//These styles will be specific only to the components on the pages, excluding the articles and policy pages.
//Typography will include only the typography that is general. Anything else will be in their respective locations for styling.

export const Display = styled.span`

`
export const PageTitle = styled.h1`

`
export const MainHeading = styled.h2`

`
export const SubHeading = styled.h3`

`
export const Title = styled.h4`

`
export const SubTitle = styled.h5`

`
export const Paragraph = styled.p`

`
export const Overline = styled.span`
  
`
export const UnorderedList = styled.ul`

`
export const ListItem = styled.li`

`