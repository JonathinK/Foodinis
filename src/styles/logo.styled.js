import styled from "styled-components";
import { NavLink } from "./header.styled";
//---

//Footer and Header Logo styles will be in the footer and header stylesheets as default styles until actual names are injected and specific styles are loaded to prevent crazy layout shift
export const LogoContainer = styled.div`
  
  
  /*Link Wrapper For Navigation to home*/
  ${NavLink}{

  }
`